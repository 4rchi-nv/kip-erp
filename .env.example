NEXT_PUBLIC_API_MODE=mock
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: move Next.js build cache to a local disk when the project is on a slow/network drive.
# Example (Windows): NEXT_DIST_DIR=C:/Users/PC/AppData/Local/Temp/kip-erp-next
